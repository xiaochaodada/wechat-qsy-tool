<view>
    <view style="position:absolute;width:100%;height:88%;top:0;">
        <video autoplay="true" bindended="end" bindplay="play" controls="{{false}}" id="myvideo" showCenterPlayBtn="{{false}}" showProgress="{{false}}" src="{{video}}" style="position:relative;width:{{videoW}}px;height:{{videoH}}px;top:{{videoTop}}px;left:{{videoLeft}}px"></video>
        <view bindtouchend="drawEnd" bindtouchmove="drawMove" bindtouchstart="drawStart" style="z-index:10;position:absolute;width:{{videoW}}px;height:{{videoH}}px;top:{{videoTop}}px;left:{{videoLeft}}px;" wx:if="{{remark}}">
            <view catchtouchmove="WraptouchMove" catchtouchstart="WraptouchStart" data-id="{{item.id}}" style="width:{{item.width}}px;height:{{item.height}}px;position: absolute;z-index:10;left:{{item.x}}px;top:{{item.y}}px;background:rgba(246, 181, 208,0.5);border:5rpx solid rgb(212, 207, 207);" wx:for="{{itemList}}" wx:key="itemList">
                <image catchtouchstart="deleteItem" class="cancel" data-id="{{item.id}}" src="../images/guanbi.png" wx:if="{{item.active}}"></image>
                <image catchtouchmove="dragPointMove" catchtouchstart="dragPointStart" class="kuoda" data-id="{{item.id}}" src="../images/lashen.png" wx:if="{{item.active}}"></image>
            </view>
        </view>
    </view>
    <view style="width:85%;font-size:34rpx;margin:300rpx auto;padding:15rpx;border:2rpx solid #93E6C6" wx:if="{{!video}}">
        <text>使用手册：
   1.点击视频按钮选择视频
   2.在视频中画出水印的地方（在画出时会出现一个框）
   3.画好后点击确定按钮，上传视频去水印
   4.点击保存就完成了</text>
    </view>
    <view class="bottom">
        <button bindtap="getVideo">
            <image src="../images/video.png"></image>
     视频
    </button>
        <button bindtap="pauseVideo" wx:if="{{!play&&video.length>3}}">
            <image src="../images/play.png"></image>
     暂停
    </button>
        <button bindtap="playVideo" wx:if="{{play&&video.length>3}}">
            <image src="../images/pause.png"></image>
     播放
    </button>
        <button bindtap="login" data-video="video_1"wx:if="{{!save}}">
            <image src="../images/comfirm.png"></image>
      确定
    </button>
        <button bindtap="saveVideo" wx:if="{{save}}">
            <image src="../images/save.png"></image>
     保存
    </button>
    </view>
</view>
